version: '3'
services:
  nodejs:
    build:
      context: .
      dockerfile: Dockerfile
    command: node server.js
    image: nodejs
    container_name: nodejs
    ports:
      - '3000:5000'
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules